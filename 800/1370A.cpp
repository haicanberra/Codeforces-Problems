#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main()
{
    int t;
    cin >> t;

    int n, a, b;
    vector<int> gcd;
    while (t--)
    {
        cin >> n;
        cout << n / 2 << endl;
    }

    return 0;
}